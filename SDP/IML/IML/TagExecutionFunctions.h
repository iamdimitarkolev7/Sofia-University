#pragma once
#ifndef _TAGEXFUNC
#define _TAGEXFUNC

class TagExecutionFunctions
{
public:

	static int increase(const int& el, const int inc_value);
	static int multiply(const int& el, const int multiply_value);
	static int sum(const int& el, const int x);
	static int average(const int& crrSum, const int );
};

#endif
